{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "MarketData",
  "type": "object",
  "required": ["date", "market_price", "contract_price"],
  "properties": {
    "date": {"type": "string", "format": "date"},
    "market_price": {"type": "number"},
    "contract_price": {"type": "number"}
  }
}
